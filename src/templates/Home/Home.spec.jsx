import { render, screen, fireEvent } from '@testing-library/react';
// import { Home } from './Home';

describe('<Home />', () => {
  it('shoul render Home correctly', () => {
    // render(<Home />);
  });
});
